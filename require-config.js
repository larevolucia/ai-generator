require("dotenv").config();

requirejs.config({
  nodeRequire: require,
  paths: {
    // Define paths to your modules here if needed
    //  axios: "https://cdn.jsdelivr.net/npm/axios@1.6.7/dist/axios.min",
    // typewriter: "https://unpkg.com/typewriter-effect@latest/dist/core"
  }
});
